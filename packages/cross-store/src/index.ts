/**
 * @blueprime/cross-store — validated cross-store references.
 *
 * Scaffold only. The @Resolve decorator, allowed-reference registry, adapters,
 * and linked repositories land in milestone M3 (see the plan doc), with the six
 * correctness fixes to issue #124 (TOCTOU, batch-first, registry scope coverage,
 * ADAPTER_UNAVAILABLE vs NOT_FOUND, batched validator snapshot, saga/outbox).
 */
export const PACKAGE_NAME = '@blueprime/cross-store';
